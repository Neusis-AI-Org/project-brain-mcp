{
  "annotations": {
    "readOnlyHint": true,
    "title": "Get knowledge base config"
  },
  "description": "Understand the project's knowledge base structure, available documentation paths, and rules. Call this at the start of a session before using other KB tools — the KB structure varies per project, so always discover it first.",
  "inputSchema": {
    "properties": {
      "ref": {
        "description": "Git ref (branch or tag). Defaults to the repository's default branch.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "name": "kb_config"
}